# Environment variables for local testing with PostgreSQL
# Copy this to .env and update with your actual PostgreSQL URL from Render

# PostgreSQL connection (replace with your Render PostgreSQL URL)
DATABASE_URL=postgresql://username:password@host:port/database_name

# Production settings
SECRET_KEY=your-secret-key-here
NODE_ENV=production
DATA_DIR=/opt/render/project/src/data

# For local development, you can use a local PostgreSQL instance:
# DATABASE_URL=postgresql://localhost/webwoz_dev
